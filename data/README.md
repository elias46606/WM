# /data

Laufzeit-Daten, die Jarvis selbst schreibt (z.B. `briefing.json` vom
täglichen Ingestion-Endpoint). Wird nicht eingecheckt — siehe
`.gitignore`. Beim ersten Deploy/Start ist dieses Verzeichnis leer,
das ist normal.
